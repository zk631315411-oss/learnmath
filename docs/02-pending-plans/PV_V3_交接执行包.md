# 学数有道 PV · V3 修订交接执行包

> 日期：2026-08-26 晚
> 依据：`PV_FIXLIST_V3.md`（导演逐条拍板）；红线与流程见 `PV_SCRIPT_FINAL.md` v7.1、`PV_PRODUCTION_PLAN.md`
> 现状基线：`artifacts/pv/学数有道-PV.mp4`（110s 粗剪，V2 漂移已修，素材齐）
> 一句话：本包是"拿到就能干"的清单。已拍板的不再问；只有**小狸声线来源**一项待定（导演晚点给），不阻塞其他工作。**补充**：b44af5383877（2.2 完全展开式）也需养成（S1b），否则 S3 首页 hero 会显示 2.2"学习中"而非 2.3"需要巩固"。

---

## 0. 先读这三条口径（别再犯）

1. **禁伪造问答/状态标签；但允许且要求正式账号真实预学习养成前态**（§3.0）。逆序数"已学过"就靠真实补养成达成，不是后期贴字。
2. **Manim 不动**。五步骨架已验收正确，正式片按现状进成片，不重触发、不改内容。
3. **"已思考"卡片任何镜头不展开**（泄漏内部教学策略）。

**账号**：正式账号 `pv_formal_20260826b`（唯一，试拍/彩排不得用它；反之试拍账号素材不得冒充正式前后态）。**禁用 kz 账号首页素材**（显示第 4 章矩阵，与行列式主线无关）。

---

## 1. 素材任务（需新产出）

| 序 | 素材 | 做法 | 状态依据 |
|----|------|------|----------|
| S1 | **逆序数"已学过"补养成** | 正式账号在 2.1 再做一次真实 independent 闭合。推荐话术："排列 312 的逆序数，我自己算的是 τ=2——3 后面 1 和 2 都比它小，两对逆序。对吗？"（严格锚定 2.1，**避开"完全展开式"表述**，turn2 探针实测该词会 resolved 到 b44af5383877）。判定：最近 2 条闭合连续 independent → 已学过。完成后只读核 `closed_evidence_count=2` 且 `status=mastered`。 | 当前逆序数 learning/1 条闭合（post-progress.json） |
| S1b | **2.2 完全展开式"已学过"养成** | S1 完成后执行。推荐话术："n 阶行列式的完全展开式，每一项是不同行不同列 n 个元素的乘积，符号由列标排列的逆序数决定。我理解得对吗？"（带答案确认式，**不提"教我"**；若系统先讲授落 direct_taught 则账号作废回滚）。两轮确认 → 2 条连续 independent → `mastered`。 | b44af5383877 当前 learning/0 闭合 + 1 条 unresolved |
| S2 | **证据段前后态素材** | S1 完成后重取：前态（逆序数 学习中）/ 后态（逆序数 **已学过** + 两行互换 需要巩固），分屏分拍，不拼假同屏。 | 两行互换已是 needs_review/2 条闭合 |
| S3 | **正式账号首页素材** | 录 `pv_formal_20260826b` 首页：顶部"2.3 行列式的性质 **需要巩固**"（MapHome.tsx hero 行）+ "该巩固了：上次学过，但没完全掌握"提示 + 章节行红色"1个需巩固"徽标。**前置：S1/S1b 完成后执行**——若 b44af5383877 未养成，首页 hero 会显示"2.2 n阶行列式的定义 · 学习中"而非 2.3 需要巩固（`actionableRank`: needs_review(0) < learning(1)，但 2.2 learning 无前置节点时可能排在更前）。供连续性段。 | 真实后态首页 |
| S4 | **定位段 KG 卡片镜头** | 从正式录屏补取"展开已检索知识图谱卡片"片段（命中：两行互换行列式反号/支撑知识：n阶行列式）。 | 制作计划 §3.2 步骤 7 |
| S5 | **开头句 TTS**（旁白声线 Huihui） | 新文案："这个答案，学生看完就懂了吗？"44.1kHz wav。 | 替换旧 01-hook |
| S6 | **小狸口播** | "学数有道，你的 AI 数学导师。" ≤10s、44.1kHz、声线区别旁白。**⏸ 声线来源待定（导演晚点给），暂缓**。 | — |
| S7 | recording.png 小狸"拿笔记录"姿态 | ✅ **已完成**（`artifacts/pv/character/cutout/recording.png`，证据段用）。 | — |
| S8 | **08/09 重录** | `08-continuity.wav` → "下次回来，系统还记得你学到哪里。"；价值句只做字幕不出声（V3 第 14 条），现有 `09-ending.wav` 弃用。Huihui 声线，44.1kHz。 | 旧 08 文案"AI 教师小狸，记得你学到哪里。"与首页画面不搭 |

## 2. 剪辑/合成任务（改现有脚本与时间码）

| 序 | 改动 | 要点 |
|----|------|------|
| E1 | **开头重排** | 新开头句（S5）在答案流式呈现后（约 6s）进；L1/L2 字幕同步后移并对齐配音实测波形（以 ffprobe 语音起止为准，不手填）。hook 段总长按需微调。 |
| E2 | **配音/字幕对位** | 配音 delay 与字幕两表合一；全片各段旁白与画面事件对齐（含第 6/7 条联动）。 |
| E3 | **04 段因果顺序** | 学生第二轮"展开式不太熟…"气泡**在先**，旁白"学生的回答，决定下一步先讲哪里"**在后**（落于气泡呈现后）。 |
| E4 | **"小题"对应画面** | 旁白"用一道小题，确认学生掌握情况"落在该轮回答**末尾探测题**"排列 312 的逆序数是多少？"入镜时；画面滚到教师出题处。 |
| E5 | **50s 学生问句** | 剪/改学生气泡为自然问句（如"回到行列式变号问题，为什么交换两行，行列式变号呢？"）。学生话非系统台词可改写；**Manim 仍用现有真实链路产物，不换**。 |
| E6 | **定位段可视化** | 旁白"先定位知识点"落在 KG 卡片展开画面（S4）。狐狸姿态/朝向不改。 |
| E7 | **22-26s 地图段状态** | 主线"两行互换行列式反号"此时=**未探索**、逆序数=学习中（前态正确口径）；主线不得显示"学习中"。2.1 梯子展开镜头后移到证据段做前态。 |
| E8 | **连续性段** | 闪回（0.3s 白闪/模糊→清晰）→ 正式账号首页（S3）全景 → 轻推顶部"需要巩固"状态条，停 2-3s。**弃用 `composite/continuity-dialogue.png` 合成图**。 |
| E9 | **zoompan 卡顿** | 全片静帧推/拉统一修：`minterpolate` 补帧或提高 zoompan 的 fps/采样，消除抖动。 |
| E10 | **收尾** | 小狸口播句（S6，待定）+ 价值句"把每道题放回知识体系，把每次学习留在学习地图上。"**只做字幕不出声**（不再压一行、不再旁白念出）。品牌卡沿用。 |

## 3. 验收点（做完逐条核）

1. 开头：答案流完 → 6s 左右才进"这个答案，学生看完就懂了吗？"，字幕与配音对齐。
2. 22-26s：主线=未探索、逆序数=学习中，无 2.1 梯子节点级画面。
3. 定位段：旁白"先定位知识点"与 KG 卡片展开同框。
4. 04 段：学生第二轮气泡先于"学生的回答…"旁白；"小题"旁白与探测题 312 同框。
5. 证据段：逆序数 学习中→已学过、两行互换 需要巩固，分屏分拍、变化感清晰。
6. 连续性段：闪回 → 正式账号首页"需要巩固"，无合成对话图、无 kz 素材。
7. 全片 zoompan 丝滑无卡顿。
8. 收尾：小狸口播品牌句（若声线已定）+ 价值句纯字幕无声，109s 前收完。
9. 红线：无虚构系统台词、无伪造状态标签、无"已思考"卡片、Manim 为现有真实链路产物。

## 4. 排期建议（对齐赛事节点 8/27 粗剪、8/28-29 终版、8/31 截止）

**排期确认（2026-08-26 深夜）**：8/27 粗剪 = **准终版**（用户明确）。S1/S1b → S2/S3/S4 串行链路 + E1-E10 全做，明天交付。

1. 先做 S1 逆序数补养成 → S1b 完全展开式养成 → S2/S3/S4 录素材。
2. 并行做 S5 开头句 TTS + 08/09 重录；S6 等声线。
3. 素材齐后跑 E1-E10 剪辑合成。
4. 按 §3 验收点抽帧核对，出终版。
