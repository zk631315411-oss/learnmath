services:
  api:
    build:
      context: ..
      dockerfile: deploy/backend/Dockerfile
  web:
    build:
      context: ..
      dockerfile: deploy/frontend/Dockerfile
      args:
        VITE_PV_BILIBILI_URL: ${VITE_PV_BILIBILI_URL:-}
  manim-renderer:
    build:
      context: ..
      dockerfile: deploy/manim/Dockerfile
