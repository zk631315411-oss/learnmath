# 教材 PDF 是外部资产（约 440MB），不进 git
data/textbooks/*.pdf
frontend/public/*.pdf

# git worktree（backend 是独立工作树，内容走 backend-dev 分支）
backend/

# 依赖与构建产物
node_modules/
dist/
venv/
__pycache__/
*.pyc

# 环境变量（密钥不入库）
.env

# 日志与临时文件
*.log
tmp/
